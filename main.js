const tabs = document.querySelectorAll('.tab');
const contents = document.getElementById('plans-content');

const plans = {
    basic: {
        label: 'Basic',
        title: 'Icon Sets',
        price: '$60',
    }
}